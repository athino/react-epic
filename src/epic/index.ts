import { createActions } from './exports/createActions'
import { createState } from './exports/createState'

/**
 * Utility to create global state for your app.
 */
export const epic = {
   createActions,
   createState
}
